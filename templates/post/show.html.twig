{% extends('base.html.twig') %}
{% block body %}
    <div class="container">
        <div class="row">
            
                <div class="col-sm-4">
                    <h3>{{post.title}}</h3>
                    <p>{{post.description}}</p>
                </div>
            
        </div>
    </div>
{% endblock %}